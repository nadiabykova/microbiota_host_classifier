# microbiota_host_classifier
A random forest model to detect pet samples in human microbiome data

To repeat analysis,

1) unpack data.tar.gz
2) make sure the following modules are installed:

3) run ./launch.sh script, it will make folders with results (tables and figures) and .joblib RF models
